<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @emoji.name %>
</p>

<p>
  <strong>Url:</strong>
  <%= @emoji.url %>
</p>

<%= image_tag @emoji.image.url(:thumb) %>

<%= link_to 'Edit', edit_emoji_path(@emoji) %> |
<%= link_to 'Back', emojis_path %>
